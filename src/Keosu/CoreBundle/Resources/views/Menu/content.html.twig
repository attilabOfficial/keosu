{% for content in contents %}
    <div class="sidebarlink">
        <a class="sous-menu" href="{{ path('keosu_'~content~'_viewlist') }}">
            <div class="lien-menu">
                <span class='glyphicon glyphicon-{{ icon[content] }} icon-menu'></span><span
                        class="texte-lien-menu">{{ content }}</span>
            </div>
        </a>
    </div>
{% endfor %}
